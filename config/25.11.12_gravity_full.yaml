pipelines:
  - pipeline: form-inertial
    name: naive_init
    prior_scale: 0.005
    planar_sigma: 0.01
    sweep:
      point_feats_per_sector: [0, 3]

  - pipeline: form-inertial
    name: form_init
    prior_scale: 0.005
    planar_sigma: 0.01
    sweep:
      point_feats_per_sector: [0, 3]

  - pipeline: form
    name: lo_planar
    sweep:
      point_feats_per_sector: [0, 3]

  - pipeline: form-inertial
    name: form_opt
    prior_scale: 0.001
    planar_sigma: 0.001
    sweep:
      point_feats_per_sector: [0, 3]

datasets:
  # - name: oxford_spires_custom/blenheim_palace_02
  #   length: 6000
  # - name: oxford_spires_custom/blenheim_palace_02
  #   length: 6000
  - name: newer_college_2020/short_experiment
    length: 6000
